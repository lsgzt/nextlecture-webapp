import { createApp } from "./app";

/**
 * Bundled into `api/index.js` by `build:vercel`. Vercel deploys this generated
 * file as one function, keeping all project-local server modules together.
 */
const app = createApp();

export default app;
